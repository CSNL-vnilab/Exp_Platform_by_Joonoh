# Prototype-distortion categorization (R)

Genre: **categorization**
Framework: **custom (R)**
IV (within-session block-kind): `block_kind` ∈ {training, test}  (4 train + 2 test)
IV (per-trial): `orientation` (continuous, ±45°)
DVs: response, correct, rt
Saved: `data/cat_results.csv`

This fixture exercises the analyzer's ability to recognise within-session block-kind partitioning (training vs test) without it being PTB-flavoured.
